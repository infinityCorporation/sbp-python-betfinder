version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.11
    commands:
      - echo "Installing dependencies"
      - /var/app/venv/staging-LQM1lest/bin/pip install -r build/requirements.txt
  pre_build:
    commands:
      - echo "Dependencies installed"
  build:
    commands:
      - echo "Python WGSI, skipping build..."
  post_build:
    commands:
      - echo "Build complete, pushing to EC2"
